!!!
html
  head
    :stylus

      $base-text-color = fade-out(black, 0.25)

      body
        font 16pt Helvetica
        padding 30px
        margin 0
        color $base-text-color

      .logo
        color rgba(0,0,0,.5)
        margin-top 0
        .from
        .version
          color rgba(0,0,0,.25)

      .links
        padding 0
        list-style-type none

      // a:link    {color:blue;}
      // a:link:visited {color:green;}
      // a:hover   {color:red;}
      // a:active  {color:yellow;}

      a
        color rgba(0,0,0, .5)
        // background fade-out(black, 0.9)
        // font-weight 900
        text-decoration: none
        &:active
          color black
          // font-weight 900
          text-decoration: none
        &:hover
          text-decoration: underline
        &:visited
          color rgba(0,0,0, .5)
          background yellow
          // display: none


  - bareLink = function(l){return '<a href="' + l + '">' + l + '</a>'}

  body
    h1.logo hello
      .from from
      | loadr
      .version= version

    //- hr

    //- p GitHub repository: !{bareLink("https://github.com")}
    //- p Documentation: !{bareLink("http://githsdsdub.com")}

    ul.links
      li: a(href="https://github.com/Wizek/loadr") documentation
      li: a(href="https://github.com/Wizek/loadr") github repository
      br
      li: a(href="/cache") cache status

    //- h2= version
    //- pre= JSON.stringify(settings,0,2)

